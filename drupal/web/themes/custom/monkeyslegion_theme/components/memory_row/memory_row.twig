{# Memory Row — Canvas admin preview #}
<div data-component="memory_row" style="display: flex; padding: 0.6rem 0; border-bottom: 1px solid rgba(255,255,255,0.05); color: white; font-size: 0.85rem;">
  <div style="flex: 1; font-family: monospace; color: #a78bfa;">{{ strategy|default('ConversationMemory') }}</div>
  <div style="flex: 2; color: #9ca3af;">{{ use_case }}</div>
</div>
